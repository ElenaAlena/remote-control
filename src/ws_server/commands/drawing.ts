export const drawCircle = async (
  note: string,
  radius: number
): Promise<void> => {
  console.log(`${note}`);
};

export const drawRectangle = async (
  note: string,
  width: number,
  height: number
): Promise<void> => {
  console.log(`${note}`);
};
